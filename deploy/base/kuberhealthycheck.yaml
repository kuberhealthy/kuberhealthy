---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: kuberhealthychecks.kuberhealthy.github.io.kuberhealthy.github.io
spec:
  group: kuberhealthy.github.io.kuberhealthy.github.io
  names:
    kind: KuberhealthyCheck
    listKind: KuberhealthyCheckList
    plural: kuberhealthychecks
    shortNames:
    - khc
    - khcheck
    - kuberhealthycheck
    singular: kuberhealthycheck
  scope: Namespaced
  versions:
  - name: v2
    schema:
      openAPIV3Schema:
        description: KuberhealthyCheck is the Schema for the kuberhealthychecks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KuberhealthyCheckSpec defines the desired state of KuberhealthyCheck
            properties:
              name:
                description: Foo is an example field of KuberhealthyCheck. Edit kuberhealthycheck_types.go
                  to remove/update
                type: string
              podSpec:
                description: PodSpec   v1.PodSpec              `json:"podSpec"`
                x-kubernetes-preserve-unknown-fields: true
              singleRunOnly:
                type: boolean
              status:
                description: KuberhealthyCheckStatus defines the observed state of
                  KuberhealthyCheck
                properties:
                  additionalMetadata:
                    type: string
                  currentUUID:
                    type: string
                  errors:
                    items:
                      type: string
                    type: array
                  lastRunUnix:
                    format: int64
                    type: integer
                  namespace:
                    type: string
                  ok:
                    description: |-
                      INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                      Important: Run "make" to regenerate code after modifying this file
                    type: boolean
                  runDuration:
                    type: string
                required:
                - additionalMetadata
                - currentUUID
                - errors
                - lastRunUnix
                - namespace
                - ok
                - runDuration
                type: object
            required:
            - name
            - status
            type: object
            x-kubernetes-preserve-unknown-fields: true
          status:
            description: KuberhealthyCheckStatus defines the observed state of KuberhealthyCheck
            properties:
              additionalMetadata:
                type: string
              currentUUID:
                type: string
              errors:
                items:
                  type: string
                type: array
              lastRunUnix:
                format: int64
                type: integer
              namespace:
                type: string
              ok:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: boolean
              runDuration:
                type: string
            required:
            - additionalMetadata
            - currentUUID
            - errors
            - lastRunUnix
            - namespace
            - ok
            - runDuration
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
