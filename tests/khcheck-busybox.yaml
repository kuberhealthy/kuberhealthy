apiVersion: kuberhealthy.github.io/v2
kind: KuberhealthyCheck
metadata:
  name: test-kh-check
  namespace: kuberhealthy
spec:
  singleRunOnly: true
  podSpec:
    spec:
      containers:
        - image: busybox
          command: ["sh", "-c", "exit 0"]
          name: main
      restartPolicy: Never
