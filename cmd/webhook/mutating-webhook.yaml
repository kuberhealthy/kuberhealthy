apiVersion: admissionregistration.k8s.io/v1beta1
kind: MutatatingWebhookConfiguration
metadata:
  name: kuberhealthy-mutating-webhook
  labels:
    app: kuberhealthy
webhooks:
  - name: kuberhealthychecks.comcast.com
    matchPolicy: Equivalent
    clientConfig:
      service:
        name: kuberhealthy-admission-webhook
        namespace: kuberhealthy
        path: "/mutate"
    rules:
      - operations: ["CREATE", "UPDATE"]
        apiGroups: ["comcast.github.io"]
        apiVersions: ["v1"]
        resources: ["khchecks"]
    # namespaceSelector:
    #   matchLabels:
    #     kuberhealthy-admission-webhook: enabled